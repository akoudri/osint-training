version: '3.8'

services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n_osint_monitor
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=ChangeMePlease! # Pensez à mettre un mot de passe fort
      - GENERIC_TIMEZONE=Europe/Paris # Important pour la synchro des logs et CRON 
      - TZ=Europe/Paris
      # Pour activer le tunnel n8n (utile si vous devez recevoir des Webhooks externes plus tard)
      # - N8N_TUNNEL_SUBDOMAIN=mon-osint-local 
    volumes:
      - n8n_data:/home/node/.n8n
    # Commande optionnelle pour lancer le tunnel directement si besoin :
    # command: /bin/sh -c "n8n start --tunnel"
    networks:
      - osint_network
    
volumes:
  n8n_data:

networks:
  osint_network:
    driver: bridge